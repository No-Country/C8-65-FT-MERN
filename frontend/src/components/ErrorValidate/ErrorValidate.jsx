import React from 'react'

const ErrorValidate = ({ menssage }) => {
    return (
        <span className='text-[0.9rem] md:text-[1rem] text-celeste self-center'>{menssage}</span>
    )
}

export default ErrorValidate